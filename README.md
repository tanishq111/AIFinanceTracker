1. To run do npm install in both the folders fronted & backend, then do npm run dev on both the folders
2. Fix these bugs
3. Bugs
1. Fix the transaction Filter, if expense is selected category should only show respective category
2. Implement a store to maintain categories consistent accross
3. Task -> add a delete & update button for budgets (backend code is ready)
customHooks to make api calls
4. Currency is not in sync with the user choice it's by default dollar(done for dashboard page, for rest all todo)
5. AI insights are not getting cleared.
